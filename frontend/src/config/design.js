// src/config/design.js

/**
 * @description 设计稿尺寸配置
 * @property {number} DESIGN_WIDTH - 设计稿宽度（按目标分辨率 4096x2160 设计）
 * @property {number} DESIGN_HEIGHT - 设计稿高度
 *
 * 注意：在开发环境（1920x1080）会看到缩小版本
 *       在目标环境（4096x2160）会看到 1:1 原始尺寸
 */
export const DESIGN_WIDTH = 1920
export const DESIGN_HEIGHT = 1080
